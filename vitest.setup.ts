/**
 * Next.js читает .env сам, отдельно запущенный vitest — нет. Тестам,
 * которые ходят в базу, нужен DATABASE_URL, поэтому загружаем его здесь.
 */

import { existsSync } from 'node:fs'

if (existsSync('.env')) process.loadEnvFile('.env')

/**
 * Канал почты в тестах — всегда файловый ящик, чем бы ни был занят `.env`.
 *
 * Тесты рассылки проверяют факт появления письма нужному адресату и создают
 * для этого людей с адресами вида `letters-test-…@example.com`. Стоит
 * разработчику переключить `.env` на настоящий канал — и прогон начнёт
 * ломиться наружу с этими адресами. Это не только упавшие тесты: отказы
 * по несуществующим адресам портят репутацию домена, с которого потом
 * уходят письма живым подписчикам.
 *
 * Провайдеры проверяются отдельно и поднимают собственный стенд, переменную
 * они выставляют сами внутри теста.
 */
process.env.MAIL_PROVIDER = 'file'
