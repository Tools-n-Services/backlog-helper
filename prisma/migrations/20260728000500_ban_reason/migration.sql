-- Причина блокировки.
--
-- Показывается самому заблокированному: блокировка без объяснения
-- неотличима от удаления аккаунта, и человек уходит вместо того, чтобы
-- исправить поведение (FR-204).
ALTER TABLE "app_user" ADD COLUMN "ban_reason" TEXT;
