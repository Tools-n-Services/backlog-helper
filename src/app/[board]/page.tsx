import { PlannedScreen } from '@/ui/layout/planned-screen'

export default function BoardPage() {
  return (
    <PlannedScreen
      iteration="A1"
      title="Лента доски"
      what="Сортировки, мультифильтр по статусу, типу и категории со счётчиками, состояние в URL, «Показать ещё»."
    />
  )
}
