import { PlannedScreen } from '@/ui/layout/planned-screen'

export default function RoadmapPage() {
  return (
    <PlannedScreen
      iteration="A4"
      title="Что делаем"
      what="Колонки по статусам со всех досок, лимит с «показать все», фильтры по доске и категории."
    />
  )
}
