import { PlannedScreen } from '@/ui/layout/planned-screen'

export default function LoginPage() {
  return (
    <PlannedScreen
      iteration="A5"
      title="Вход"
      what="Вход по ссылке из письма и отдельный экран «ссылка отправлена» — состояние, а не тост."
    />
  )
}
