import { defineConfig } from '@playwright/test'

export default defineConfig({
  testDir: './e2e',
  /* Пересев перед прогоном: сценарии теперь пишут в базу, и без него
     второй запуск идёт по данным, оставленным первым. */
  globalSetup: './e2e/global-setup.ts',
  fullyParallel: true,
  use: {
    baseURL: 'http://localhost:3000',
    /* Русский браузер: язык интерфейса выбирается по Accept-Language,
       и без этого Chromium со своим en-US открывал бы портал по-английски,
       а сценарии ищут русские подписи (FR-181). */
    locale: 'ru-RU',
  },
  webServer: {
    command: 'pnpm dev',
    url: 'http://localhost:3000',
    reuseExistingServer: true,
    env: {
      /* Письма в файловый ящик: сценарии входят по настоящей ссылке. */
      MAIL_PROVIDER: 'file',
      /* Короткая жизнь снимка: сценарии правят справочник и настройки
         в базе и тут же смотрят на портал, а ждать десять секунд значит
         добавить в прогон паузы, которые ничего не проверяют. Ноль ставить
         нельзя — тогда каждый запрос лезет в базу за неизменным. Голосование
         перерисовывает страницу на каждый клик (revalidatePath), и на коротком
         снимке шесть быстрых кликов подряд начинают ловить собственную
         медлительность вместо ошибок портала. */
      CATALOG_TTL_MS: '1000',
      /* Мастер установки без токена в среде не существует вовсе —
         значит прогону токен нужен (В4). */
      INSTALL_TOKEN: 'e2e-install-token',
    },
    timeout: 60_000,
  },
})
